name: settings_api
description: An abstract class and models for an API providing access to settings.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
